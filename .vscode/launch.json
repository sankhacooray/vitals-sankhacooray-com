{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Serve vitals (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:4492",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "serve:vitals"
    }
  ]
}
